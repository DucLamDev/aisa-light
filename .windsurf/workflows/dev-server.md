---
description: Start the Next.js development server
---

# Start Dev Server

1. Check if port 3000 is already in use
// turbo
2. Run `npm run dev` from the project root to start the Next.js dev server on port 3000
3. Open browser preview at http://localhost:3000
